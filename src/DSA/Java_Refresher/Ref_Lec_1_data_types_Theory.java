package DSA.Java_Refresher;

public class Ref_Lec_1_data_types_Theory {
    public static void main(String[] args) {
        /*
        Numbers are divided into 4 data types ->
        1. Integer
        2. Long
        3. Float
        4. Double
        On a number line ->
        Integer stores -> +ve and -ve whole numbers { -3, -2, 1, 4. 0 }
        Long stores -> bigger than integer
        Float stores -> +ve and -ve decimal numbers
        Double stores -> with higher precision than float

        string -> anything that is stored in double quotes is a String

        1. Integer -> -2^31 to 2^31 or -10^9 to 10^9
        2. Long ->    -2^63 to 2^63 or -10^18 to 10^18
        3. Float ->    32 bits = 4 bytes
        4. double ->   64 bits { more precision }  64 bits = 8 bytes
    =======================================================================================================
        Typecasting = changing data type of the variable
        Ex ->
        -> container A has capacity of 50 ml of water and container B has capacity pf 100 ml of water
        -> water Transfer from A to B -> can be done easily -> no spillage
        -> water transfer from B to A -> with spillage  { Overflow }

        -> we can migrate -> from Integer to Long -> with no data loss
        -> we cant migrate -> from Long to Integer -> with data loss  { Overflow }
    ========================================================================================================
         */
    }
}
